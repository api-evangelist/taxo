generated: '2026-07-21'
method: derived
status: candidate
source: https://taxo.co/ec/productos/taxo-api
note: >-
  No official hosted/remote MCP server was found for Taxo. This is a CANDIDATE
  tool list derived from the operations documented on the Taxo API product page
  and developer docs (docs.taxo.ws). No public OpenAPI spec exists to bind
  operationIds; tools map to documented REST operations.
server:
  name: taxo
  transport: http
tools:
- name: create_invoice
  description: Issue an electronic invoice (factura) for authorization by the SRI.
  source_operation: POST /v1/invoices
- name: get_invoice
  description: Retrieve an invoice/document and its authorization status.
  source_operation: GET /v1/invoices/{id}
- name: create_credit_note
  description: Issue a credit note (nota de crédito).
  source_operation: POST /v1/credit-notes
- name: create_retention
  description: Issue a retention (retención).
  source_operation: POST /v1/retentions
- name: list_documents
  description: List issued electronic documents.
  source_operation: GET /v1/documents
- name: validate_document
  description: Pre-validate document data before submission to the SRI.
  source_operation: POST /v1/validate
